---
layout: layout.html
---
<h1>{{ page.title }}</h1>
<h2>{{ page.date | date: '%A, %B %e @ %l:%M %P' }}</h2>

<dl class="schedule_skate-info">
  {% if page.start %}
    <dt>Start</dt>
    <dd>{{ page.start }}</dd>
  {% endif %}
  {% if page.end %}
    <dt>End</dt>
    <dd>{{ page.end }}</dd>
  {% endif %}
  {% if page.leader %}
    <dt>Leader</dt>
    <dd>{{ page.leader }}</dd>
  {% endif %}
  {% if page.distance %}
    <dt>Distance</dt>
    <dd>{{ page.distance }}</dd>
  {% endif %}
  {% if page.highlights %}
    <dt>Highlights</dt>
    <dd>{{ page.highlights }}</dd>
  {% endif %}
</dl>